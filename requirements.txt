# RAG Pipeline — Dependencies
# Install: pip install -r requirements.txt

# Core ML / Embeddings
faiss-cpu>=1.7.4
sentence-transformers>=2.7.0
numpy>=1.26.0

# LangChain (document loaders + text splitter)
langchain>=0.2.0
langchain-community>=0.2.0
pypdf>=4.0.0          # PDF loading

# LLM Provider (Groq — free tier, fast)
groq>=0.9.0

# Web UI (optional)
streamlit>=1.35.0

# Dev / Testing
pytest>=8.0.0
python-dotenv>=1.0.0
